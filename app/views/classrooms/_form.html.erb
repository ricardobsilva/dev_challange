<%= form_for(@classroom) do |f| %>
  <% if @classroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classroom.errors.count, "error") %> prohibited this classroom from being saved:</h2>

      <ul>
      <% @classroom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.label 'estudante'%>
    <%= f.collection_select(:student_id, @students, :id, :name , {prompt: "escolha um aluno"} , {class: "form-control"} ) %>
    <%= f.label 'curso'%>
    <%= f.collection_select(:course_id, @courses, :id, :name , {prompt: "escolha um curso"} , {class: "form-control"} )  %>
    <%= f.hidden_field :entry_at , :value => Time.now%>
    </br>
    <%= f.submit "cadastrar", class: 'btn btn-primary' %>
  </div>
  </br>
<% end %>
